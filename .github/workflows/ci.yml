name: CI Pipeline - Práctica

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  validate:
    name: Validaciones Básicas
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      # Paso 1: Descargar el código
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1  # Solo descargar el último commit

      # Paso 2: Verificar estructura del proyecto
      - name: Verify Files
        run: |
          echo "📂 Estructura del proyecto:"
          ls -la
          
          echo "\n🔍 Contenido de archivos importantes:"
          [ -f README.md ] && head -5 README.md || echo "⚠️ No existe README.md"
          [ -f index.html ] && head -5 index.html || echo "⚠️ No existe index.html"

      # Paso 3: Pruebas simuladas
      - name: Run Tests
        run: |
          echo "🧪 Iniciando pruebas..."
          echo "1. Verificando sintaxis básica"
          [ -f index.html ] && grep -q '<html' index.html && echo "✅ HTML válido" || echo "⚠️ No se detectó HTML válido"
          
          echo "\n2. Conteo de archivos"
          echo "Total: $(find . -type f | wc -l) archivos"
          
          echo "\n🎉 Pruebas completadas"

      # Paso 4
      - name: Create Artifact
        run: |
          timestamp=$(date +%Y%m%d-%H%M%S)
          echo "Versión: $timestamp" > build-info.txt
          echo "Commit: ${{ github.sha }}" >> build-info.txt
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: build-metadata
          path: build-info.txt

